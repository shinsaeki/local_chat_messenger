# local_chat_messenger
A playground repository for experimenting with inter-process communication (IPC) using TCP, UDP, and UNIX domain sockets. Includes simple client-server implementations for local message exchange.
